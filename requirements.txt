# Python依赖 (用于演示版本)
# 主要依赖通过外部工具实现

# 核心依赖
requests>=2.28.0
mutagen>=1.45.0  # FLAC元数据处理

# 增强功能依赖（推荐安装）
qrcode[pil]>=7.3.1  # 本地二维码生成
tkinter  # GUI界面（通常随Python一起安装）

# 开发依赖
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0